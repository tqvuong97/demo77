<%= form_for @post do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this product from being saved:</h2>
      <ul>
        <% @post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :conclusion %><br>
    <%= f.text_field :conclusion %>
  </div>
   <div class="field">
    <%= f.label :aboutauthor %><br>
    <%= f.text_field :aboutauthor%>
  </div>
  

   <div class="actions">
    <%= f.submit "save" %>
  </div>
  <div class="actions">
    <%= f.submit "back" %>
  </div>
  <div class="actions">
    <%= f.submit "Publish" %>
  </div>
<% end %>